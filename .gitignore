# Environment
.env

# Workspace — binary samples and analysis output are NOT committed
workspace/binaries/*
workspace/ghidra-projects/*
workspace/reports/*

# Keep the folder structure
!workspace/binaries/.gitkeep
!workspace/ghidra-projects/.gitkeep
!workspace/reports/.gitkeep

# Skills are physically bundled — all 42 RE specialist skills tracked in git
# skills/ is fully committed — no setup script needed

# OS
.DS_Store
Thumbs.db
